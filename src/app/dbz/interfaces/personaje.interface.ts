

// las interfaces no se importan en el modulo,
// ya que cuando se hace el build no son nada
// practicamente

// interface de personaje
// se define el tipo de dato que se espera en la lista
export interface Personaje {
  nombre: string;
  poder: number;
}

